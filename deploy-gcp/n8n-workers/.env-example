# --- Proyecto y región ---
PROJECT_ID=your-project-id
REGION=us-central1
ENVIRONMENT=dev
# --- Servicios de Cloud Run ---
CLOUDRUN_SERVICE_NAME=n8n-main
WORKER_SERVICE_NAME=n8n-workers

# --- Cuenta de servicio usada por Cloud Run ---
N8N_SERVICE_ACCOUNT_NAME=n8n-deploy-sa

# --- PostgreSQL (Cloud SQL) ---
DB_INSTANCE_NAME=n8n-postgresql
DB_NAME=n8n
DB_USER=n8n
DB_PASSWORD=changeme
DB_SECRET_NAME=n8n-db-password

# --- Redis (Memorystore) ---
REDIS_INSTANCE_NAME=n8n-redis

# --- Seguridad N8N ---
N8N_ENCRYPTION_KEY=changeme-32-characters-please

# --- Nodos customizados (opcional) ---
GITHUB_USER=tu_usuario_github
GITHUB_TOKEN=tu_token_github
